diff --git a/src/logging.c b/src/logging.c
index e7647fb..d06a8e5 100644
--- a/src/logging.c
+++ b/src/logging.c
@@ -1,7 +1,13 @@
-/* logging.c — implementacion de utilidades de tiempo y CSV. */
+/* logging.c — implementacion de utilidades de tiempo, CSV y trazas. */
 #include "logging.h"
 #include <stdio.h>
+#include <stdlib.h>
 #include <time.h>
+#include <math.h>
+
+struct GapTrace {
+    FILE *f;
+};
 
 double wall_seconds(void) {
     struct timespec ts;
@@ -26,3 +32,46 @@ void csv_append(const char *path, const char *instance, int N, int M, int p,
             opt_known, status);
     fclose(f);
 }
+
+static void print_num(FILE *f, double v) {
+    if (isfinite(v)) fprintf(f, "%.6f", v);
+    else fprintf(f, "NA");
+}
+
+GapTrace *gap_trace_open(const char *path) {
+    if (!path || !*path) return NULL;
+    FILE *probe = fopen(path, "r");
+    int exists = 0;
+    if (probe) { exists = 1; fclose(probe); }
+
+    FILE *f = fopen(path, "a");
+    if (!f) return NULL;
+    if (!exists) {
+        fprintf(f, "phase,iteration,elapsed_time,LB,UB,gap,cuts_added,lazy_cuts,nodes,total_cuts,separation_calls\n");
+    }
+    GapTrace *gt = malloc(sizeof(GapTrace));
+    if (!gt) { fclose(f); return NULL; }
+    gt->f = f;
+    return gt;
+}
+
+void gap_trace_close(GapTrace *gt) {
+    if (!gt) return;
+    if (gt->f) fclose(gt->f);
+    free(gt);
+}
+
+void gap_trace_log(GapTrace *gt, const char *phase, long iteration,
+                   double elapsed_time, double LB, double UB, double gap,
+                   long cuts_added, long lazy_cuts, double nodes,
+                   long total_cuts, long separation_calls) {
+    if (!gt || !gt->f) return;
+    fprintf(gt->f, "%s,%ld,", phase ? phase : "NA", iteration);
+    print_num(gt->f, elapsed_time); fprintf(gt->f, ",");
+    print_num(gt->f, LB); fprintf(gt->f, ",");
+    print_num(gt->f, UB); fprintf(gt->f, ",");
+    print_num(gt->f, gap); fprintf(gt->f, ",");
+    fprintf(gt->f, "%ld,%ld,", cuts_added, lazy_cuts);
+    print_num(gt->f, nodes); fprintf(gt->f, ",%ld,%ld\n", total_cuts, separation_calls);
+    fflush(gt->f);
+}
diff --git a/src/logging.h b/src/logging.h
index cf9d5a9..8a6ad01 100644
--- a/src/logging.h
+++ b/src/logging.h
@@ -1,7 +1,9 @@
-/* logging.h — utilidades de tiempo y registro CSV (brief seccion 9 del kickoff). */
+/* logging.h — utilidades de tiempo, CSV y trazas experimentales. */
 #ifndef PMP_LOGGING_H
 #define PMP_LOGGING_H
 
+typedef struct GapTrace GapTrace;
+
 double wall_seconds(void);   /* reloj monotonico en segundos */
 
 /* Agrega (append) una fila al CSV de benchmark; crea cabecera si el archivo no existe.
@@ -12,4 +14,12 @@ void csv_append(const char *path, const char *instance, int N, int M, int p,
                 long iter, double nodes, double Ttot,
                 const char *opt_known, const char *status);
 
+/* Traza gap/curva por iteracion/callback. Append-only: no pisa trazas previas. */
+GapTrace *gap_trace_open(const char *path);
+void      gap_trace_close(GapTrace *gt);
+void      gap_trace_log(GapTrace *gt, const char *phase, long iteration,
+                        double elapsed_time, double LB, double UB, double gap,
+                        long cuts_added, long lazy_cuts, double nodes,
+                        long total_cuts, long separation_calls);
+
 #endif
diff --git a/src/phase1.c b/src/phase1.c
index 9c027b4..0a2e2d0 100644
--- a/src/phase1.c
+++ b/src/phase1.c
@@ -42,7 +42,7 @@ static void p1_add_cut(int client, const Cut *cut, void *user) {
 }
 
 Phase1Result phase1_run(const Instance *inst, const SortSites *ss, int verbose,
-                        CutPool *out_pool) {
+                        CutPool *out_pool, GapTrace *trace) {
     int N = inst->N, M = inst->M;
     Solver *s = solver_create(1);
 
@@ -71,6 +71,7 @@ Phase1Result phase1_run(const Instance *inst, const SortSites *ss, int verbose,
     int *roundset = malloc(inst->p * sizeof(int));
 
     int iter = 0;
+    double t0 = wall_seconds();
     while (iter < P1_MAXIT) {
         iter++;
         ctx.added_this_iter = 0;
@@ -81,16 +82,24 @@ Phase1Result phase1_run(const Instance *inst, const SortSites *ss, int verbose,
             res.UB1 = ub;
             for (int t = 0; t < inst->p; t++) res.best_set[t] = roundset[t];
         }
-        if (verbose)
-            printf("[F1] iter=%d LB1=%.4f UB1=%.1f cuts+=%d total=%ld\n",
-                   iter, res.LB1, res.UB1, ctx.added_this_iter, ctx.ncuts);
 
-        if (ctx.added_this_iter == 0) break;   /* no hay cortes violados => fin */
+        int cuts_added = ctx.added_this_iter;
+        if (cuts_added != 0) {
+            solver_optimize(s);
+            res.LB1 = solver_objval(s);
+            solver_get_x(s, 0, M, ybar);
+            solver_get_x(s, M, N, thetabar);
+        }
+
+        double gap = (isfinite(res.UB1) && res.UB1 > 0.0) ? (res.UB1 - res.LB1) / res.UB1 : NAN;
+        gap_trace_log(trace, "phase1", iter, wall_seconds() - t0, res.LB1, res.UB1,
+                      gap, cuts_added, 0, 0.0, ctx.ncuts, iter);
+
+        if (verbose)
+            printf("[F1] iter=%d LB1=%.4f UB1=%.1f gap=%.6f cuts+=%d total=%ld\n",
+                   iter, res.LB1, res.UB1, gap, cuts_added, ctx.ncuts);
 
-        solver_optimize(s);
-        res.LB1 = solver_objval(s);
-        solver_get_x(s, 0, M, ybar);
-        solver_get_x(s, M, N, thetabar);
+        if (cuts_added == 0) break;   /* no hay cortes violados => fin */
     }
 
     res.iters = iter;
diff --git a/src/phase1.h b/src/phase1.h
index be05218..4cc6e10 100644
--- a/src/phase1.h
+++ b/src/phase1.h
@@ -5,6 +5,7 @@
 #include "instance.h"
 #include "sortsites.h"
 #include "cutpool.h"
+#include "logging.h"
 
 typedef struct {
     double LB1;       /* cota inferior = relajacion LP del maestro */
@@ -17,6 +18,6 @@ typedef struct {
 /* Corre Fase 1. El llamador libera result.best_set.
  * Si out_pool != NULL, acumula ahi el pool de cortes generados (para warm-start). */
 Phase1Result phase1_run(const Instance *inst, const SortSites *ss, int verbose,
-                        CutPool *out_pool);
+                        CutPool *out_pool, GapTrace *trace);
 
 #endif
diff --git a/src/phase2.h b/src/phase2.h
index 9e3f3d3..7de7bdb 100644
--- a/src/phase2.h
+++ b/src/phase2.h
@@ -6,6 +6,7 @@
 #include "instance.h"
 #include "sortsites.h"
 #include "cutpool.h"
+#include "logging.h"
 
 typedef struct {
     double  objval;     /* optimo probado */
@@ -20,6 +21,6 @@ typedef struct {
  * warm != NULL => precarga ese pool de cortes (warm-start, metodo del paper);
  * warm == NULL => arranque limpio (solo callback). */
 Phase2Result phase2_run(const Instance *inst, const SortSites *ss, int verbose,
-                        const CutPool *warm);
+                        const CutPool *warm, GapTrace *trace);
 
 #endif
diff --git a/src/solver.h b/src/solver.h
index fe4033e..2457fba 100644
--- a/src/solver.h
+++ b/src/solver.h
@@ -49,6 +49,9 @@ void solver_set_lazy_callback(Solver *s, solver_lazy_fn fn, void *user);
 /* dentro del callback: copia la solucion entera completa (todas las vars) en out
  * (out debe tener tamano solver_num_vars). */
 void solver_cb_get_solution(SolverCB *cb, double *out);
+/* metricas disponibles dentro del callback MIPSOL; valores no disponibles => NaN. */
+void solver_cb_get_metrics(SolverCB *cb, double *runtime, double *objbst,
+                           double *objbnd, double *nodecnt);
 /* agrega un corte perezoso: sum val[k]*x[ind[k]] sense rhs */
 void solver_cb_add_lazy(SolverCB *cb, int len, const int *ind, const double *val,
                         char sense, double rhs);
diff --git a/src/solver_gurobi.c b/src/solver_gurobi.c
index c69792b..28bc44e 100644
--- a/src/solver_gurobi.c
+++ b/src/solver_gurobi.c
@@ -6,6 +6,7 @@
 #include "solver.h"
 #include <stdio.h>
 #include <stdlib.h>
+#include <math.h>
 #include "gurobi_c.h"
 
 struct Solver {
@@ -20,6 +21,7 @@ struct Solver {
 struct SolverCB {
     Solver *s;
     void   *cbdata;
+    int     where;
 };
 
 static void die(Solver *s, const char *ctx) {
@@ -113,7 +115,7 @@ static int __stdcall grb_cb(GRBmodel *model, void *cbdata, int where, void *usrd
     (void)model;
     Solver *s = usrdata;
     if (where == GRB_CB_MIPSOL && s->lazy_fn) {
-        SolverCB cb = { s, cbdata };
+        SolverCB cb = { s, cbdata, where };
         s->lazy_fn(&cb, s->lazy_user);
     }
     return 0;
@@ -134,6 +136,21 @@ void solver_cb_get_solution(SolverCB *cb, double *out) {
         die(cb->s, "cbget");
 }
 
+static void cbgetdbl_or_nan(SolverCB *cb, int what, double *out) {
+    if (!out) return;
+    double v = NAN;
+    if (!GRBcbget(cb->cbdata, cb->where, what, &v)) *out = v;
+    else *out = NAN;
+}
+
+void solver_cb_get_metrics(SolverCB *cb, double *runtime, double *objbst,
+                           double *objbnd, double *nodecnt) {
+    cbgetdbl_or_nan(cb, GRB_CB_RUNTIME, runtime);
+    cbgetdbl_or_nan(cb, GRB_CB_MIPSOL_OBJBST, objbst);
+    cbgetdbl_or_nan(cb, GRB_CB_MIPSOL_OBJBND, objbnd);
+    cbgetdbl_or_nan(cb, GRB_CB_MIPSOL_NODCNT, nodecnt);
+}
+
 void solver_cb_add_lazy(SolverCB *cb, int len, const int *ind, const double *val,
                         char sense, double rhs) {
     if (GRBcblazy(cb->cbdata, len, ind, val, sense, rhs)) die(cb->s, "cblazy");
